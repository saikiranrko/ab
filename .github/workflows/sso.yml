name: Multiline Paragraph Display

on:
  workflow_dispatch:
    inputs:
      message:
        description: >
          📘 Enter a full paragraph here.

          Paste a multiline paragraph and we'll display it cleanly in logs and the summary.
        required: true

jobs:
  display-paragraph:
    runs-on: ubuntu-latest

    steps:
      - name: Set input as environment variable
        run: |
          echo "MESSAGE<<EOF" >> $GITHUB_ENV
          echo "${{ github.event.inputs.message }}" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Display paragraph using Python
        run: |
          python3 <<EOF
import os

paragraph = f'''📘 Paragraph Input Received:

-------------------------------------
{os.getenv("MESSAGE", "").strip()}
-------------------------------------
'''

print(paragraph)
EOF

      - name: Add paragraph to GitHub Step Summary
        run: |
          echo '### 📘 Paragraph Input Summary' >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ github.event.inputs.message }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
