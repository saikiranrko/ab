name: Multiline Paragraph Display

on:
  workflow_dispatch:
    inputs:
      message:
        description: >
          📘 Enter a full paragraph here.

          Paste a multiline paragraph and we’ll show it nicely in the logs and the summary.
        required: true

jobs:
  display-paragraph:
    runs-on: ubuntu-latest

    steps:
      - name: Display paragraph using Python
        env:
          MESSAGE: ${{ github.event.inputs.message }}
        run: |
          python3 <<EOF
paragraph = f"""📘 Paragraph Input Received:

-------------------------------------
{os.environ['MESSAGE']}
-------------------------------------
"""
print(paragraph)
EOF

      - name: Add paragraph to GitHub Step Summary
        run: |
          echo "### 📘 Paragraph Input Summary" >> $GITHUB_STEP_SUMMARY
          echo '```${{ github.event.inputs.message }}```' >> $GITHUB_STEP_SUMMARY
