name: Process Multiline Input with Python
on:
  workflow_dispatch:
    inputs:
      message:
        description: >
          📘 Paste your multiline text here.
          When you paste text, it will be converted into the correct multiline format.
        required: true
jobs:
  process-multiline:
    runs-on: ubuntu-latest
    steps:
      - name: Save input to file
        run: |
          echo '${{ github.event.inputs.message }}' > input.txt
      
      - name: Process with Python
        run: |
          python3 -c '
import os

# Read the input file
with open("input.txt", "r") as f:
    message = f.read()

# Process the multiline input
formatted_message = message.splitlines()
formatted_output = "\n".join(formatted_message)

# Print the formatted output
print("📘 Paragraph from input:")
print("-----------------------------")
print(formatted_output)
print("-----------------------------")
          '
      
      - name: Add to GitHub Step Summary
        run: |
          {
            echo "### 📘 Paragraph from input"
            echo "<pre>"
            cat input.txt
            echo "</pre>"
          } >> $GITHUB_STEP_SUMMARY
