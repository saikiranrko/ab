name: Test Input Description

on:
  workflow_dispatch:
    inputs:
      message:
        description: |
          📝 This is a test description.

          Enter any message here to test how input descriptions show up in the GitHub UI.
        required: true
        default: "Hello from the input!"

jobs:
  echo-input:
    runs-on: ubuntu-latest

    steps:
      - name: Print the input message
        run: echo "🚀 Message: ${{ github.event.inputs.message }}"
